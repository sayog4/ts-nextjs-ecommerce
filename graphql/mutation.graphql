mutation Signup($data: SignUpInput!) {
  signUp(data: $data) {
    name
    email
    phone
  }
}
mutation Logout {
  logOut
}
mutation Login($data: LogInInput!) {
  logIn(data: $data) {
    name
    email
    phone
  }
}
